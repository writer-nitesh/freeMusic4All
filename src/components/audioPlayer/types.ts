export interface PlayerProperties {
    startTime: number
    currentTime: number
    duration: number
    isFullScreen: boolean
    isAudioPlaying: boolean
    volume: number
    playerData: any
    musicId: string
}

